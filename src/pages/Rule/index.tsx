import React from 'react';

import { Container } from '@mui/material';

const Rule = () => (
    <Container sx={{ py: 2 }} maxWidth='xl'>
        <p>Rule Container</p>
    </Container>
);

export default Rule;
